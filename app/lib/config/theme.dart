import 'package:flutter/material.dart';

const grey_ = Color.fromRGBO(141, 139, 141, 1);

// const blackTheme_ = Color.fromRGBO(29, 37, 44, 1);

const whiteTheme_ = Color.fromRGBO(255, 255, 255, 1);

const pink_ = Color.fromRGBO(253, 73, 92, 1);
const blackTheme_ = Color.fromRGBO(32, 41, 69, 1);

final ThemeData themeData = ThemeData(
    colorScheme: ColorScheme.fromSeed(seedColor: grey_),
    useMaterial3: true,
    fontFamily: 'Outfit');
